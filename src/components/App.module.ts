import { Module } from '../core/module-decorator';
import BootstrapContainer from '../core/BootstrapContainer';

@Module({
    imports: [],
    providers: [],
})
export default class AppModule extends BootstrapContainer {

}
